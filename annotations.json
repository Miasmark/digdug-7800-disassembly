{
  "_README": [
    "Started by init.py from Dig Dug (NTSC) (Atari) (1987) (50CB13F3).a78.",
    "",
    "Every human judgement about this ROM lives in this file; the",
    "generated listings are disposable, so renaming is free.",
    "",
    "What to do next, roughly in order:",
    "",
    "  1. python tools/disasm.py Dig Dug (NTSC) (Atari) (1987) (50CB13F3).a78 -c annotations.json",
    "     Read the coverage it reports. Low coverage is normal at",
    "     first and is the number you are trying to move.",
    "",
    "  2. Every unresolved indirect jump it reports is a hole. Add the",
    "     target to `entries`, or the pointer pair to `ram_vectors` if",
    "     the handler is installed into RAM.",
    "",
    "  3. python tools/audiotrace.py Dig Dug (NTSC) (Atari) (1987) (50CB13F3).a78 -c annotations.json",
    "     Finds the sound code and the tables feeding it.",
    "",
    "  4. Name things in `labels` as soon as you know what they are.",
    "",
    "See docs/method.md for the working order and docs/pitfalls.md for",
    "the traps that each cost real time."
  ],
  "entries": [
    "rom:EED8",
    "rom:C15F",
    "rom:D000"
  ],
  "ram_vectors": [
    ["0x1FBE", "0x1FBF"]
  ],
  "bankat": {},
  "labels": {
    "rom:EED8": "ENTRY_Irq",
    "rom:C15F": "ENTRY_Nmi",
    "rom:D000": "ENTRY_Reset"
  },
  "ram": {
    "9E": "LivesRemaining",
    "A2": "ScoreLo",
    "A4": "ScoreMid",
    "A6": "ScoreHi",
    "87": "CurrentPlayer",
    "A0": "NewGameFlag",
    "2600": "TerrainMap",
    "9B": "LevelNumber"
  },
  "comments": {
    "rom:D057": "CONFIRMED: the boot-time RAM init. Doesn't clear whole pages the way Centipede/Ballblazer's does -- clears specific, narrow ranges directly (ram_007E-0088, ram_0040-0052 paired with ram_24C0-24D2, a few ram_24C9-24D2 sub-fields) rather than one big page-sweep loop. Also copies dat_EACC/dat_C008 (both inside the confirmed chr_rom_C000/gfx_C000 region) into ram_2400,Y -- a display-list-adjacent init, not yet fully traced.",
    "rom:DF7D": "CONFIRMED (SED at rom:DF80, BCD arithmetic; live-checked against tools/probe-ram-snapshots.lua, run-01.inp): the score-add routine. Adds into ScoreLo/Mid/Hi,X (X = CurrentPlayer) with carry chained across all three BCD bytes, then checks the new high-digit value (CMP #$02, AND #$0F/BEQ, CMP #$05) -- matching the manual's extra-life rule (20,000 and 50,000, then every 50,000 after: https://atariage.com/manual_html_page.php?SoftwareID=2126) almost exactly by shape, though the precise digit-place arithmetic hasn't been traced byte-by-byte. On a match, falls into rom:DF9E which increments LivesRemaining,X (capped at 10) and calls sub_DD3B (also called from the death-recovery path at rom:DC98 -- likely a shared 'lives display update' or 'life gained/restored' notification, not yet named).",
    "rom:D298": "CONFIRMED: terrain/dirt map level-init. Computes LevelNumber,X (X=CurrentPlayer; confirmed as a level/round number by rom:sub_DF33's level-clear check, which increments it) mod 10 via repeated SBC #$0A, then fills a large span of TerrainMap with one of three fixed values ($20/$22/$1E) depending on the result -- a level-dependent dirt pattern. rom:sub_D2C0 (called right before, rom:D256) does a second pass: fills TerrainMap+15 through the page end with $24 (a border/edge value, matching sub_D660's own $24 threshold), then carves specific already-open cells ($4C, plus $4A into the adjacent ram_25FF page) at fixed positions from dat_E7BF (the same position table sub_D16C's display-list builder uses) -- likely the level's starting tunnel entrance(s).",
    "rom:D660": "CONFIRMED: the terrain-cell classifier. Reads TerrainMap,Y and buckets it against the exact threshold values the level-init code (rom:D298) fills the map with: >= $24 (border/solid) returns a fixed $0F; < $1E (already-dug/open) returns the value halved; the $1E-$23 band (packed dirt) returns a fixed $02. Called from rom:sub_F952 in a loop stepping through cells along a movement direction -- this is collision/movement-clamping logic (how far can an object move before hitting solid terrain), not the dig action itself. The dig action is rom:sub_D8E2/rom:sub_FD43 -- see those comments.",
    "rom:C30A": "CONFIRMED, found via a PC-tagged write-tap on TerrainMap (tools/probe-terrain-writes.lua, run-01.inp) that led here indirectly (see rom:D8E2): converts a pixel (X,Y) position to a TerrainMap index and reads the cell. Y-pixel >>3 (row, ram_00CC); X-pixel >>2 indexes ram_2490,X (a row-to-table-index lookup), which indexes dat_E7BF,X (the same per-row base-offset table sub_D16C's display-list builder and the level-init code both use) to get the row's TerrainMap base offset; adding ram_00CC's row-component gives the final index. `LDA TerrainMap,X` at the end reads the cell -- callers get the raw terrain byte back in A.",
    "rom:D8E2": "CONFIRMED, found via a PC-tagged write-tap on TerrainMap (tools/probe-terrain-writes.lua, run-01.inp) that pointed straight at rom:sub_FD43's callers: the dig-check-and-erode routine. Looks up a movement-transition table (one of dat_E1D9/E1DD/E1CF/E1D6/..., selected by the current direction/turn state in Y) to pick which of several erosion tables (dat_E1B3/E1B7/E1BB/...) to use for this step, stores that table's pointer to ram_00CF, computes the target cell's position (rom:sub_C30A, screen position plus a per-direction offset from dat_D6F3/dat_E0DC), and -- only if the cell isn't already solid/border (CMP #$24, BCS skips the dig) -- calls rom:sub_FD43 to erode it. Checks a second, related cell the same way right after (using ram_0080 and dat_E1BB), consistent with a tunnel opening affecting more than one cell per step.",
    "rom:FD40": "CONFIRMED (see rom:D8E2 for the caller chain that found this): the actual TerrainMap write-back -- `sub_FD43`'s `(old_value-$24)>>1` indexes a small lookup table (pointed to by ram_00CF, set per-direction by the caller) to get the *new* terrain value, then stores it into TerrainMap,X. This is the dig action itself: repeatedly digging the same cell walks it through a short sequence of intermediate 'partially dug' values (rather than clearing to open in one step), though the exact table contents (which values, how many steps) aren't decoded yet.",
    "rom:F952": "CONFIRMED shape: per-object movement-limit check. Reads a 2-bit direction out of ram_0080 (ROR/ROR into carry+sign), then walks cells in that direction via rom:sub_D660, calling rom:sub_E1F7 (accumulates ram_009D += 4 per open cell -- a distance-so-far counter, not the dig action; a tiny 4-byte routine, easy to mistake for something bigger) for each passable cell until the classifier's bitmask test fails. Not yet confirmed which object this applies to (the player specifically, or any moving object using the same movement code) or exactly how ram_009D's accumulated distance gets used afterward.",
    "rom:DF33": "CONFIRMED: the level-clear check. Loops X=7..0 over ram_00B2,X (bit 7 = alive, an 8-slot status array -- likely the enemy table, shared or parallel to the object tables used elsewhere) looking for any live one; if none are found (all 8 enemies dead), plays a sound/animation (rom:sub_FDEF, rom:sub_DD4C) then increments LevelNumber,X (X = CurrentPlayer, rom:DF4E) and re-runs the DLL setup (rom:sub_D1BE) and terrain-map init (rom:sub_D244) for the next level -- LevelNumber is exactly the ram_009B,X value rom:D298's terrain-init reads to pick the level's dirt pattern.",
    "rom:DC41": "CONFIRMED LIVE (tools/probe-ram-snapshots.lua, run-01.inp): the death-handling routine. Plays a short animation loop (rom:L_DC54, cycling a sprite/character index through dat_C155 with a fixed delay), then decrements LivesRemaining,X (X = CurrentPlayer, rom:DC6F). If lives hit zero, checks the *other* player's LivesRemaining (Y = CurrentPlayer XOR 1, rom:DC85-DC8D) before deciding the game is really over (rom:sub_DD10) -- correctly handles 2-player mode not sharing a single life pool, even though this project's own recording never exercised 2-player play. If lives are still available (either this player's or, in a shared-lives 2P mode, the other's), falls through to rom:sub_DD3B instead -- the same 'life gained/restored' routine the extra-life award path calls. LIVE: LivesRemaining ($009E) counts cleanly 5->4->3->2->1->0 three separate times across the whole recording, resetting to 5 each time (frames 1980, 12540, 24300) in exact lockstep with NewGameFlag ($00A0) pulsing 0->1->0 and ScoreHi ($00A6) resetting to 0 -- a new game starting, matching the manual's '5 starting lives' exactly.",
    "rom:C15F": "ENTRY_Nmi is `JMP ram_1FBD` -- NOT indirect, a plain absolute jump to a fixed RAM address. CONFIRMED self-modifying: boot code (rom:sub_D057) writes $4C (the JMP opcode itself) to ram_1FBD, then the target's low/high bytes to ram_1FBE/ram_1FBF, assembling a literal `JMP $target` instruction in RAM at $1FBD-$1FBF. Declared ram_vectors on the [ram_1FBE, ram_1FBF] pair (the scanner doesn't need to know about the fixed $4C at $1FBD -- it only tracks the two address bytes) rather than trying to special-case self-modifying code in the tool.",
    "rom:EE57": "VEC_EE57, found via the ram_1FBE/1FBF ram_vectors scan. A single, self-contained multi-stage DLI handler -- not a ping-pong chain between separate routines like Centipede's. Each stage sets a WSYNC then writes a MARIA palette-color-register pair (P5C1/P5C2, P6C1/P6C3, P7C1/P7C3 -- distinct color sets, presumably per screen band) via a shared `STA ram_1FBE / BNE L_EE8E` trampoline that re-arms the *same* routine at a different internal offset for the next interrupt. This is why only one external target was found: every re-arm points back inside code already being traced, so the scan has nothing further to discover."
  },
  "headers": {
    "rom:C54A": "Renders cleanly at --direct 22 --lines 16 as legible bonus-point-value digit/text graphics (readable '...000' point values and what looks like a word fragment) -- NOT yet cross-checked against the manual's exact veggie/kill bonus table (400-8000 per https://atariage.com/manual_html_page.php?SoftwareID=2126). Now also confirmed live -- see the rom:C162 and rom:C362 blocks -- as part of the same graphics region this address sits inside.",
    "rom:C162": "CONFIRMED, root cause found: the ~940-reference $B900-$CFFF dataset from an early pass (tools/live-slots.lua, borrowed verbatim from Centipede) was largely garbage, but not for the reason first guessed. Two distinct bugs, found with tools/dpph-history.lua (logs every DPPH/DPPL change with its frame number) and tools/live-slots-diag.lua (tags every out-of-range reference with the frame/DPPH/DPPL/zone that produced it): (1) DPPH holds a bogus boot-time value ($1F, paired with one-off DPPL values $84/$5D) from frame 16 to frame 165 before settling into the real, stable base ($23xx, with DPPL alternating $5D/$A2 every single frame -- a genuine double-buffer, not a bug) -- fixed with a 200-frame settle gate in live-slots.lua (a first attempt at 120 frames wasn't long enough, verified against this same recording before trusting the larger number). (2) A smaller, ongoing torn-read artifact survives the settle fix -- a minority of walked entries (340 of 1072, all landing below $C000, several independently confirmed to fall on already-traced real code when treated as 16K-mirrored ROM addresses) are still garbage on some frames, most likely a genuine emulation-timing race between when the CPU updates a display-list entry and when the once-per-frame Lua callback reads it. Not chased further into MAME's own frame-timing internals -- filtered out instead (discard every reference below $C000, since that's not real ROM space regardless of cause) the same way Centipede's PC-tagged DMA-misattribution reads were filtered by 'impossible as a real address/PC', rather than trusted. The remaining 732 references are dense and consistent across every page from $C000 to $CFFF, and this range holds no already-confirmed code except ENTRY_Nmi's own 3 bytes at $C15F ($C000-$C15E and $C162-$C24C were already-open gaps; sub_C24D and the C300-C361 code block were already correctly traced) -- exactly the shape of a real, contiguous graphics sheet with two small code islands, the same pattern the other two projects' character sheets had. Declared as two blocks either side of what's already correctly traced (rom:C000-rom:C15F is a pre-existing gap not yet block-declared, still open -- see docs/FINDINGS.md for why it wasn't included this pass).",
    "rom:C362": "Continuation of the rom:C162 graphics region, resuming after sub_C24D (real code) and the already-confirmed dat_C25C/dat_C25E data and the C300-C361 code block. Ends exactly at $D000 (ENTRY_Reset, real code) with a clean transition -- L_C361's RTS at $C361, then raw zero-padded bytes with no further code or existing labels all the way to $D000. This boundary was already correctly found by the static tracer alone, before any live evidence existed; the live data (dense references throughout) confirms it rather than having been needed to find it."
  },
  "blocks": [
    {
      "loc": "rom:C000",
      "end": "rom:C15F",
      "gfx": true,
      "name": "gfx_C000",
      "note": "CONFIRMED LIVE (43 references, all in the $C078-$C0FE sub-range, tools/live-slots.lua post-fix) plus already-correct static data (dat_C000/C008/C031/C035/C038/C03A/C03B/C04E/C100/C155, each with a real xref) covering the rest -- one block spanning all of it, same family as gfx_C162/gfx_C362, since it's evidently the same resource. Ends exactly where ENTRY_Nmi's 3-byte JMP begins."
    },
    {
      "loc": "rom:C162",
      "end": "rom:C24D",
      "gfx": true,
      "name": "gfx_C162",
      "note": "CONFIRMED LIVE (tools/live-slots.lua, run-01.inp, post-fix -- see the header note). Ends where sub_C24D (real code) begins."
    },
    {
      "loc": "rom:C362",
      "end": "rom:D000",
      "gfx": true,
      "name": "gfx_C362",
      "note": "CONFIRMED LIVE (tools/live-slots.lua, run-01.inp, post-fix). Includes the C54A bonus-digit graphics. Ends exactly at ENTRY_Reset ($D000, real code) with no gap -- see the header note."
    },
    {
      "loc": "rom:C25C",
      "end": "rom:C300",
      "name": "dat_C25C",
      "note": "CONFIRMED: dat_C25C (2 bytes, xref rom:DA00) and dat_C25E (161 bytes, xref rom:D97E) already rendered correctly as data via the default fallback; declared to stop it showing as an unclaimed gap. Sits between sub_C24D and sub_C300 (both real code)."
    },
    {
      "loc": "rom:DFFF",
      "end": "rom:E0C4",
      "gfx": true,
      "name": "chr_rom_E000",
      "note": "CONFIRMED three ways: (1) CHARBASE=$E0 by code (rom:D065, rom:DD93, both load #$E0 immediately before the write); (2) gfx.py's default indirect-mode render at --base 0xE000 shows legible 'ATARI' text and a full 0-9 digit set -- see docs/FINDINGS.md; (3) tools/live-slots.lua's post-fix data (30 references) lands entirely within $E000-$E0A8, cleanly avoiding sub_E0C4 (real code, $E0C4-$E0DB) rather than scattering across it the way stale/garbage reads would -- real evidence the walker's indirect-mode handling isn't as suspect here as first worried, not just a coincidence. Checked for the exact bug that bit chr_rom_C000 in the Centipede project before declaring anything: grepped for JSR/JMP into any dat_E0/E1 label, found nothing, so this boundary excludes the code island rather than swallowing it. Starts one byte before CHARBASE's own $E000 base, at $DFFF, because that's where the preceding code (L_DFFE, RTS) actually ends and this data actually begins -- not indicative of anything about CHARBASE itself, MARIA still reads from $E000."
    },
    {
      "loc": "rom:E0DC",
      "end": "rom:E1E9",
      "gfx": true,
      "name": "chr_rom_E0DC",
      "note": "Continuation of chr_rom_E000, resuming after sub_E0C4 (real code, ends at $E0DB). Ends where sub_E1E9 (real code) begins. Not yet independently re-checked with a live display-list walk (see rom:E000-family header note in docs/FINDINGS.md on why the walker's indirect-mode handling is itself in question) -- declared on the strength of the code-boundary evidence and the same CHARBASE confirmation, not on a live reference landing specifically in this sub-range."
    }
  ],
  "notes": {
    "rom": "Dig Dug (NTSC) (Atari) (1987) (50CB13F3).a78\nmapper linear, 16 KB, NTSC\nno POKEY declared; audio is the TIA's two voices\n\nVectors: IRQ $EED8  NMI $C15F  RESET $D000\n\nWritten by init.py. Everything here is read from the cartridge; the\njudgement calls are yours to add."
  }
}
